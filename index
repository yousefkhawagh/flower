<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>I Love You Ba2lawa</title>
    <style>
        /* CSS styling */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
            background: linear-gradient(135deg, #ffe6e6, #ffd1dc);
        }

        h1 {
            font-size: 3em;
            color: #ff6b6b;
            text-shadow: 2px 2px 10px rgba(0, 0, 0, 0.2);
            opacity: 0;
            animation: fadeIn 2s forwards, heartbeat 1.5s infinite ease-in-out;
        }

        /* Fade-in effect */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* Heartbeat animation */
        @keyframes heartbeat {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.1); }
        }

        /* Button styling */
        #surpriseButton {
            position: absolute;
            bottom: 20px;
            padding: 10px 20px;
            font-size: 1em;
            background-color: #ff6b6b;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>
<body>
    <h1>I love you ba2lawa</h1>
    <button id="surpriseButton">Surprise Me!</button>

    <script>
        // JavaScript to add interactivity
        document.getElementById('surpriseButton').addEventListener('click', function() {
            alert('You are the sweetest ba2lawa!');
        });
    </script>
</body>
</html>

